# Contributing to Auto Disable Screen Reader

This project was AI-generated by **Maxim Mazurok** using **Google Antigravity with Gemini 3 Flash**.

## Local Development Setup

1.  Clone the repository:
    ```bash
    git clone https://github.com/Maxim-Mazurok/vscode-dictation-auto-disable-screen-reader-plugin.git
    cd vscode-dictation-auto-disable-screen-reader-plugin
    ```
2.  Install dependencies:
    ```bash
    npm install
    ```
3.  Compile the TypeScript code:
    ```bash
    npm run compile
    ```

## Debugging

1.  Open the project in VS Code.
2.  Press **F5** to launch the "Extension Development Host".
3.  In the new window, toggle `editor.accessibilitySupport` to see it revert.

## Packaging

To manually package the extension into a `.vsix` file:
```bash
npx @vscode/vsce package
```

## Publishing

Refer to [PUBLISHING.md](./PUBLISHING.md) for details on Marketplace publishing.
